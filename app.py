import streamlit as st
from streamlit_gsheets import GSheetsConnection
import pandas as pd
from datetime import datetime

# --- การตั้งค่าเบื้องต้น ---
st.set_page_config(page_title="ระบบสารวัตรนักเรียน", page_icon="👮‍♂️", layout="wide")

# เชื่อมต่อ Google Sheets
conn = st.connection("gsheets", type=GSheetsConnection)

# --- ส่วนของแถบข้าง (Sidebar) สำหรับ Login ---
st.sidebar.title("🔒 ส่วนของเจ้าหน้าที่")
password = st.sidebar.text_input("กรอกรหัสผ่านเพื่อดูข้อมูล", type="password")

# กำหนดรหัสผ่าน (คุณครูสามารถเปลี่ยน '1234' เป็นรหัสที่ต้องการได้ครับ)
ADMIN_PASSWORD = "admin_patwit" 

# --- ส่วนควบคุมการแสดงผล ---
if password == ADMIN_PASSWORD:
    # --- หน้าจอสำหรับเจ้าหน้าที่ (Admin Dashboard) ---
    st.title("📋 รายงานเหตุการณ์ทั้งหมด")
    st.info("เฉพาะเจ้าหน้าที่สารวัตรนักเรียนที่เข้าถึงหน้านี้ได้")
    
    try:
        # อ่านข้อมูลจาก Sheets
        df = conn.read()
        
        # แสดงตารางข้อมูล
        if not df.empty:
            st.dataframe(df, use_container_width=True)
            
            # ปุ่มดาวน์โหลดข้อมูลเป็น CSV
            csv = df.to_csv(index=False).encode('utf-8-sig')
            st.download_button("📥 ดาวน์โหลดรายงาน (CSV)", data=csv, file_name="report.csv", mime="text/csv")
        else:
            st.write("ยังไม่มีการแจ้งเหตุในขณะนี้")
            
    except Exception as e:
        st.error("ไม่สามารถดึงข้อมูลได้ กรุณาตรวจสอบการตั้งค่า Sheets")

else:
    # --- หน้าจอสำหรับนักเรียนแจ้งเหตุ (Public Form) ---
    st.title("👮‍♂️ ระบบแจ้งเหตุ - สารวัตรนักเรียน")
    st.markdown("กรุณากรอกข้อมูลตามความเป็นจริง ข้อมูลของคุณจะถูกเก็บเป็นความลับ")

    with st.form(key="incident_form"):
        reporter = st.text_input("ชื่อผู้แจ้ง (ระบุหรือไม่ก็ได้)")
        incident_type = st.selectbox("ประเภทเหตุการณ์", ["ทะเลาะวิวาท", "สารเสพติด/บุหรี่", "พฤติกรรมชู้สาว", "หนีเรียน/โดดเรียน", "อื่นๆ"])
        location = st.text_input("สถานที่เกิดเหตุ")
        details = st.text_area("รายละเอียดเหตุการณ์")
        
        submit = st.form_submit_button("ส่งข้อมูลแจ้งเหตุ")

    if submit:
        if not location or not details:
            st.warning("⚠️ กรุณากรอกสถานที่และรายละเอียดให้ครบถ้วน")
        else:
            try:
                # ดึงข้อมูลเดิมมาเพื่อต่อท้าย
                existing_data = conn.read()
                
                new_row = pd.DataFrame([{
                    "Timestamp": datetime.now().strftime("%d/%m/%Y %H:%M:%S"),
                    "Reporter": reporter if reporter else "ไม่ประสงค์ออกนาม",
                    "Incident_Type": incident_type,
                    "Location": location,
                    "Details": details,
                    "Status": "รอดำเนินการ"
                }])
                
                updated_df = pd.concat([existing_data, new_row], ignore_index=True)
                conn.update(data=updated_df)
                
                st.success("✅ ส่งข้อมูลสำเร็จ! ทีมสารวัตรนักเรียนจะรีบดำเนินการตรวจสอบ")
                st.balloons()
            except Exception as e:
                st.error(f"เกิดข้อผิดพลาดในการบันทึกข้อมูล: {e}")

# --- ส่วนท้ายหน้าเว็บ ---
st.sidebar.markdown("---")
st.sidebar.write("ระบบรับแจ้งเหตุภายในโรงเรียน")
